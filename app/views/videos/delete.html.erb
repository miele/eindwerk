<div class="inner">
				<p><%= image_tag('/stylesheets/backend/assets/icons/big_icons/video.png') %>
					<%= image_tag('/stylesheets/backend/assets/icons/big_icons/video_title.png',:id => 'big_title') %>
					</p>
										<h1>Delete the video!</h1>
									
					<%= link_to("<< Back to list", {:action => 'list'}, :class => 'button_link') %>
<div>&nbsp;</div>
<%= form_for(:video,:html => { :id => "contact_form_id" }, :url => {:action => 'destroy', :id => @video.id}) do |f| %>
	<p>Are you sure you want to delete this record?</p>
	<p class="reference-name"><%= @video.title %></p>
	<div class="form-buttons">
		<%= submit_tag("Delete Video",:class => 'button_link')%>
	</div>
<% end %>
</div>
<% content_for :head do %>
<script type="text/javascript">
    $(function() {
      $('#video').addClass('current').html("<a><span>Video's</span></a>");
    });
  </script>
<% end %>